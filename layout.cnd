constraints:
  - group: {directions: [b, e, l, o, w, right, below], selector: next, direction: horizontal}

directives:
  - size: {width: 100, height: 100, selector: IntNode}
  - attribute: {field: value}
  - projection: {sig: State}
  - flag: hideDisconnectedBuiltIns